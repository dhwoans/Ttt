/**
 * 멀티플레이 플레이어 목록 관련 사이드 이펙트 관리
 * 현재는 별도의 수신 로직 없이 컴포넌트에서 Store를 구독하는 것으로 충분함
 * (필요 시 토스트 알림 등을 위해 유지 가능)
 */
export function useMultiplayerPlayers() {
  // 현재는 데이터 동기화가 SocketManager에서 직접 이루어지므로 이 훅은 비어 있어도 무방함
  // 향후 플레이어 입장/퇴장 시 특정 사운드를 재생하는 등의 로직을 여기에 추가할 수 있음
}
